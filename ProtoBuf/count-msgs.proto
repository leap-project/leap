syntax = "proto3";
package protoBuf;

// To compile the protobuf to Python run the following inside the protoBuf folder:
// protoc -I=. --python_out=. count-msgs.proto

// Simple example query
message Query {
    string filter_logic = 1;
}